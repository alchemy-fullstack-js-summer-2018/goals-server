//const request = require('./request');
const mongoose = require('mongoose');
//const assert = require('chai').assert;

//fake test for now to pass travis
describe.skip('Goals API', () => {
    
    beforeEach(() => mongoose.connection.dropDatabase());


});